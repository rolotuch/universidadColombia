from django.urls import path


urlpatterns = [
    # path('',include(('aplicaciones.config.urls','config'), namespace='config')),
    # path('',include(('aplicaciones.home.urls','home'), namespace='home')),
    # path('admin/', admin.site.urls),
]